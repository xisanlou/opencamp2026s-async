# 1. 阅读inspect-embassy 代码

## inspect_embassy/src/model/ty.rs

1. `inspect_embassy::model::Type` 是类型标识符，用以区分文件中的各种类型定义，并且作为【Type -> FutureType】映射的key。

2. Type类型的构建入口是关联函数`Type::from_maybe_ddbug_type`，其主要输入是`ddbug_parser::Type`，所以这一函数主要完成 `ddbug_parser::Type -> inspect_embassy::model::Type` 的解析。

## inspect_embassy/src/model/task_pool.rs

1. TaskPool结构的解析**完全基于**embassy-executor中的任务池定义。这使得inspect-embassy**只适用**于：

    （1）标注属性宏`#[embassy_executor::main]`和`#[embassy_executor::task]`的情况；
    
    （2）使用embassy-futures crate 中定义的各种Select和Join结构的情况。

2. 当前TaskPool的解析要求embassy-executor **不能启用** nightly feature。

3. TaskPool的解析构造入口是`TaskPool::from_ddbug_var`方法，其主要输入是`ddbug_parser::Variable`，其名称必须是POOL。

# 2. 下周计划

1. 继续阅读inspect-embassy代码。

