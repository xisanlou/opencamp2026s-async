# 目标
1. 基础目标（必达）：完成前两个阶段的练习，即阅读并分析最新代码，撰写清晰的分析文档，并能完整执行整个流程。
2. 进阶目标（推荐）：在理解代码的基础上，对现有模块进行力所能及的改进。
3. 优秀目标（挑战）：在项目中做出具有独到见解的重大改进，或实现创新性功能。
# 任务与产出
1. 周报制度：学员需在 GitHub 建立个人仓库，每周记录学习进展与代码实践，作为过程考核依据。
2. 最终报告：训练营结束时需提交总结报告，重点展示对异步机制的理解及在项目中的具体实践成果。
# 代办事项
1. 在 GitHub 建立个人仓库，并填写在线文档链接。
2. 撰写本次会议的交流纪要及后续 6 周的个人规划文档。
3. 完成第一阶段任务（爬虫性能对比实验），对比进程、线程、协程的性能差异。
4. 确定后续交流时间（暂定下周日 15:00），并在群内回复确认。
# 技术答疑
1. 异步与抢占的兼容性：针对异步为何无抢占的疑问，向勇解释称，由于协程它需要复用堆栈，所以抢占的时候，你**不能确定堆栈里是空的**，所以这时候没有办法就正常的按照共享堆栈的方式来进行协程的调度。我们在后边同学们看到的第一个任务，去学那个200行100行的异步代码，它是可以直接使用的，把这个东西放到内核里头，你**可以理解为它就是个线程，对于线程你就可以抢占了**，如果你抢占了堆栈不复用，它就是个现场。如果你是**正常的让权，那这时候堆栈肯定是空的**，因为编译器已经保证了你这条，这样一来的话，我们就可以把这两个东西结合到一起，也就是说那个执行流在被抢占的时候，它是带着堆栈的。在主动让权的时候，它是没有堆栈的。所以说你在切换的时候？只需要加上一步，**如果他没有堆栈，你给他分配一个；有，那你就直接用它的堆栈继续干就行了**，这样的话就就可以在内核里有抢占，并且有异步。 协程因复用堆栈导致无法直接抢占，但可通过混合模型（线程+协程）解决，即在主动让权时复用堆栈，在被抢占时保留堆栈现场。
2. 硬实时调度实现：向勇指出，目前 rCore 主要使用 CFS 调度器，硬实时调度算法（如 C 语言实现）移植到 Rust 并与异步结合是可行的研究方向。有人用 C 写的嵌入式系统里，它有硬实时的调度算法，这些用 rust 语言把它翻过来，**算法翻成 rust 语言是没问题的**，然后在那里头能够保证硬实时也是没问题的，现在的**问题是它怎么和这个异步再结合起来**，如果这事儿能搞到一起，是一个很不错的，很独特的贡献。

# 第一个实验题目
1. 不是全新写一个爬虫，是给出一个网站链接的文本文件，去每个链下载一个网页，然后统计延时分布、吞吐率和内存开销。