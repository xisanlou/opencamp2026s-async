# 1. 阅读inspect-embassy 代码

### 1.1 验证task_pool.rs中的类型解析

编译并调试embassy/examples/qemu-virt-riscv64，使用GDB查看其任务池类型：

`embassy_executor::raw::TaskPool<hello::____embassy_main_task::____embassy_main_task_inner_function::{async_fn_env#0}, 1>`

`embassy_executor::raw::TaskPool<hello::__say_hello_from_task_task::__say_hello_from_task_task_inner_function::{async_fn_env#0}, 3>`

这与文件inspect_embassy/src/model/task_pool.rs中的类型预期是一致的。

### 1.2 inspect_embassy/src/model.rs

1. 这一文件主要定义DebugData结构，它包含3方面内容：

	- 执行器poll函数结尾地址。

	-  映射表Type->FutureType。

	-  任务池列表。

2. inspect-embassy只支持单执行器场景。

3. 编译后执行器poll函数名的形式：embassy_executor::raw::{impl#9}::poll::{closure#0}

# 2. 下周计划

1. 继续阅读inspect-embassy代码。

