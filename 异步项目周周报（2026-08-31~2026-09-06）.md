# 1. 阅读inspect-embassy 代码

## 1.1 inspect_embassy/src/model/future.rs

1. select和join类型的并发任务的解析：这里严格限制了命名空间只能是**embassy_futures::select** 和 **embassy_futures::join** 下的类型。并且少数不常用的类型**未实现解析**，如：`Select5, Select6, SelectSlice, Join5`。

2. 类型元数据解析和运行时值的解析是分成两条线分别进行的。元数据解析的入口是 `FutureType::from_ddbug_type`; 运行时值的解析入口是 `FutureValue::new` 。

3. 当前只支持3中类型的Future解析：AsyncFn，Select，Join（select和join必须是embassy_futures crate下的）。

4. 解析运行时值FutureValue时，通过查表获得类型对应的FutureType。

# 2. 下周计划

1. 继续阅读inspect-embassy代码。

