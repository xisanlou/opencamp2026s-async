# 1. 阅读inspect-embassy 代码

inspect-embassy依赖库ddeug_parser，要获取正确结果，两层解析都要正确，即：`DWARF -> ddbug_parser -> inspect-embassy`。在解析中有大量编译器“约定俗成”的东西，实际上编译器团队并没有给予保证，inspect-embassy在使用时是在“硬编码”，编译器本身的升级或修改可能导致解析失败。

## 1.1 inspect_embassy/src/model/async_fn.rs

这一文件的作用：已知异步函数（async fn）Future的类型元数据 `ddbug_parser::Type` 和对应**实例的字节切片**，构建出这一**异步函数Future实例的运行时快照AsyncFnValue**。

文件提供2组共6个结构体，分别是：

元数据结构：Member, State, AsyncFnType

运行时值：MemberValue, StateValue, AsyncFnValue

Member是结构体成员的元数据，包括名称、类型（ty::Type）、偏移量、大小，可以在状态机结构体中定位其成员。

State是状态机在一个时刻状态快照的元数据，每个State包含这一状态活跃的Member的列表。


# 2. 下周计划

1. 继续阅读inspect-embassy代码。

